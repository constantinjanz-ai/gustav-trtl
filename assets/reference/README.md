# Reference assets

Drop real reference photos of the garden and Gustav here for the **M5 pixel-art
pass** (and real Gustav photos for the Erinnerungen scrapbook). Optional bonus —
the game is built entirely from the text descriptions in the project brief.

Sibling folders (created as needed during later milestones):
- `../sprites/` — placeholder art now; real pixel art swapped in at M5.
- `../fonts/`  — self-hosted Silkscreen + VT323 (currently loaded via Google Fonts CDN).
- `../audio/`  — real soft chiptune track at M4 (audio is procedural for now).
